# 封面

> 本模块可单独唤起：说"帮我生成封面""按这个标题做张封面"即可，无需走完整流程；也可以接在标题定稿之后，以选定标题为大字方向生成对应平台封面。

面向受众：{{AUDIENCE}}（{{PLATFORMS}}）。封面风格要贴合账号人设——{{PERSONA}}。

**平台画幅规格**：{{COVER_SIZES}}
**情绪-配色映射**：{{COVER_EMOTION_MAP}}
**系列化固定排布**：{{COVER_SERIES_LAYOUT}}

## 一、1秒法则：封面必须传达的三件事

观众划到封面的决策时间不到1秒，一张合格的封面必须在这1秒内同时传达：

1. **话题是什么**——核心讨论对象要能被一眼认出
2. **什么情绪**——按{{COVER_EMOTION_MAP}}给出的映射，情绪基调通过画面和配色直给
3. **一句勾人的大字**——一句能在瞬间抓住注意力的话，通常是结论、冲突或问题

三件事缺一不可，生成封面前先把这三件事分别想清楚，再落成具体画面和文字。

## 二、视觉焦点与留白

- **主体要少而大**：一个主视觉元素占据画面最抓眼的位置，不堆砌多个同等重量的元素
- **给文字留白**：主体不铺满整张画面，刻意留出干净区域给大字
- **画面服务文字**：两者冲突时优先保证大字清晰可读

## 三、大字文案：4-8字最佳

- 理想长度4-8个汉字，短于4字信息量不够，长于8字在手机竖屏/信息流缩略图里会挤成一团
- 一句大字只表达一个判断或一个情绪冲击点，多层信息拆到大字+副标题两级
- 用受众的口语（参考{{AUDIENCE_VOICE}}这类表达），不用书面总结

## 四、文案与标题互补，不重复同一句话

- **不要把标题原样搬到封面上再读一遍**——如果封面大字和标题几乎是同一句话，说明其中一块广告位没发挥作用
- 常见互补结构：标题抛问题、封面给结论或冲击画面；或标题给结论、封面用更简短的情绪词/悬念词呼应
- 自查方法：把标题和封面大字放一起读一遍，若信息量和只读标题时几乎没有增量，就是重复了，方向也不能互相打架（标题看空，封面不能显得看多）

## 五、系列感：固定排布带来账号识别度

{{COVER_SERIES_LAYOUT}}

这套排布是长期资产，一次定稿后每期直接复用，不要临时现想或每期换地方——这是账号识别度最强的锚点。

## 六、情绪配色

{{COVER_EMOTION_MAP}}

配色是否准确传递这条内容想让观众感受到的情绪，是唯一判断标准，不是"这个垂类惯例上用什么颜色"。

## 七、生成脚本调用

封面用 `scripts/generate_cover.py` 调用 gpt-image-2 生成，写提示词前先明确：**这张封面的大字用的就是用户确认的那个标题（或其精炼版，只做提炼不改立场）**，再定情绪色调（参考{{COVER_EMOTION_MAP}}）和主体元素。

提示词分四块组织：

1. **画面主体（英文描述）**：主视觉元素、构图方式、光效氛围，英文描述通常生成效果更稳定
2. **图上大字（必须用中文明确写出）**：用"图中央用醒目大号中文写：'XXXX'"这类明确指令，把要出现在图上的具体文字逐字写进去，不能只用英文模糊描述
3. **风格词**：补充画面调性关键词（如 professional, high contrast, bold typography, cinematic lighting, eye-catching, 4k）
4. **排版要求**：明确要求文字清晰、不被遮挡、主体不占满画面、给文字留出空间——对应第二节留白要求

按{{COVER_SIZES}}给出的规格，逐平台调用：

```bash
python3 scripts/generate_cover.py --platform <平台> --prompt "英文画面描述+中文大字指令+风格词+排版要求" --output <文件名>.png
```

例如覆盖多平台时分别生成：

```bash
python3 scripts/generate_cover.py --platform douyin --prompt "a confident smiling portrait, cinematic lighting, high contrast; 图中央用醒目大号中文写：'具体大字内容'; bold typography, eye-catching, 4k; leave clean space for text, subject not filling the frame" --output <选题关键词>_douyin.png

python3 scripts/generate_cover.py --platform bilibili --prompt "同上英文画面描述+中文大字指令" --output <选题关键词>_bilibili.png
```

具体 `--platform` 取值和输出画幅以{{COVER_SIZES}}为准，文件名建议用选题/主体关键词命名（而非通用的 `cover.png`），方便长期归档区分。

## 八、生成后检查（不可跳过）

1. **检查中文文字是否正确**：gpt-image-2 生成的中文偶尔会出现错别字、缺字或糊字，每张图生成后都要检查图上文字是否与提示词要求一致、清晰可读，不一致就调整提示词重新生成，不能带着错字的封面交付
2. **检查固定元素位置**：{{COVER_SERIES_LAYOUT}}要求的固定元素（账号名/logo/角标）是否出现在约定位置，缺失或位置漂移要重新生成
3. **检查大字与标题是否打架**：封面立场是否和标题一致（不能标题看空、封面显得看多）

## 密钥安全（红线，不可违反）

- 真实 API key 只能写进产物目录下、被 `.gitignore` 排除的真实 `.env` 文件里，由用户自己配置
- 随产物一起提交/追踪的 `.env.example` 永远只放占位符，绝不写入真实密钥值
- **绝不硬编码真实 API key** 到 `generate_cover.py`、SKILL.md 或任何会被版本控制追踪的文件中
- **绝不提交 `.env`**——确认 `.gitignore` 已排除该文件，运行前检查一遍
